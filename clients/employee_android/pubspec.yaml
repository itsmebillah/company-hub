name: employee_android
description: "Provisional Company Hub employee Android client shell."
publish_to: none
version: 0.1.5+6

environment:
  sdk: ^3.13.1

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: 10.0.0
  firebase_core: ^4.14.0
  firebase_messaging: ^16.0.0
  http: ^1.6.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
